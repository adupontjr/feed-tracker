# Copy to .env.local and fill in. Never commit real values.

# Database (when you add persistence — e.g. Vercel Postgres / Neon / Supabase)
DATABASE_URL=

# Optional: a shared key to protect the API while it's public
FEED_API_KEY=
